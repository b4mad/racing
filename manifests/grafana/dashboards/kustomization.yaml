---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: grafana
# https://github.com/Kuadrant/kcp-glbc/pull/223#issuecomment-1169667848
# maybe there's a way to just include those dashboard.json files
configMapGenerator:
  - name: landingpage
    files:
      - landingpage.json
resources:
  - overview.yaml
  - details.yaml
  - details-crew-chief.yaml