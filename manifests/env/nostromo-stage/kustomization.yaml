apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - telegraf
  - ../../base/mosquitto

generatorOptions:
  disableNameSuffixHash: true

images:
  - name: telegraf
    newTag: "1.27.3"
