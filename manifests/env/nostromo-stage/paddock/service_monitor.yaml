---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: paddock-new
spec:
  endpoints:
    - interval: 15s
      port: django-web
      scheme: http
  namespaceSelector:
    matchNames:
      - b4mad-racing-stage
  selector:
    matchLabels:
      app.kubernetes.io/component: paddock
